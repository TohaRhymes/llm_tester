version: "3.9"

services:
  api:
    build: .
    container_name: llm-tester-api
    env_file:
      - .env
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    volumes:
      - ./data:/app/data
    command: uvicorn app.main:app --host ${HOST:-0.0.0.0} --port ${PORT:-8000}
