# Jupyter Examples

- `01_question_generation.py`: generate an exam from Markdown (provider defaults to local; set env keys for OpenAI/Yandex).
- `02_model_evaluation.py`: load an exam and compare models.

Run:
```bash
pip install -r requirements.txt
jupyter notebook examples/notebooks
```
